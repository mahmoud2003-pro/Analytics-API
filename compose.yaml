services:
    app:
        image: analytics-app:latest
        build:
            context: .
            dockerfile: Dockerfile
        environment:
            - PORT=8000
        env_file:
            - .env        
        ports:
            - "8000:8000"
